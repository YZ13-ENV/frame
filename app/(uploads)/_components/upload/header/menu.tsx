import { motion } from "framer-motion"

const SideMenu = () => {
    return (
        <motion.div initial={{ width: 0 }} animate={{ width: '100%' }} className="w-full h-full shrink-0">
            
        </motion.div>
    )
}

export default SideMenu